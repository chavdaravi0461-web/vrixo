import { AnimatedLoginClient } from "@/components/auth/animated-login-client";

export const metadata = { title: "Sign in - Vrixo" };

export default async function LoginPage() {
  return <AnimatedLoginClient />;
}
